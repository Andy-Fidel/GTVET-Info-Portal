# cPanel Deployment Configuration

This directory contains files optimized for cPanel hosting environment.

## Directory Structure

```
/home/username/
├── public_html/          (Frontend build - Vite dist)
├── api/                  (Laravel backend)
│   ├── public/
│   ├── app/
│   ├── config/
│   └── ...
└── databases/            (Database backups)
```

## Setup Instructions

See main README.md in the root directory for detailed setup instructions.
